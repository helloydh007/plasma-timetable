<?xml version="1.0" encoding="UTF-8"?>
<kcfg xmlns="http://www.kde.org/standards/kcfg/1.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.kde.org/standards/kcfg/1.0
      http://www.kde.org/standards/kcfg/1.0/kcfg.xsd" >
  <kcfgfile name=""/>

  <group name="General">
    <!-- 课表模型（JSON）：开学日、作息时间表、课程。
         用单个 JSON 字符串而不是嵌套组：KConfig XT 无法表达数组结构。 -->
    <entry name="timetableData" type="String">
      <default></default>
    </entry>
    <!-- 是否用法定节假日数据覆盖课表：休日不排课 -->
    <entry name="markHolidays" type="Bool">
      <default>true</default>
    </entry>
    <!-- 节假日数据更新方式：manual（手动，默认）| auto（自动，每 30 天检查一次） -->
    <entry name="holidayUpdateMode" type="String">
      <default>manual</default>
    </entry>
    <!-- 联网获取到的节假日数据缓存（JSON；只保存内置数据未覆盖的年份） -->
    <entry name="holidayCache" type="String">
      <default></default>
    </entry>
    <!-- 本机时钟与网络时间的差值（秒）。用户改动系统时间时，课表按
         「本机时间 + 这个差值」显示，免得看到错的那一周。0 = 未校正。 -->
    <entry name="clockOffsetSec" type="Int">
      <default>0</default>
    </entry>
    <!-- 是否把「这个时段有排课、但这一周不上」的课也画出来（灰显并标注「非本周」）。
        关掉（默认）时这些格子留空。 -->
    <entry name="showOffWeekCourses" type="Bool">
      <default>false</default>
    </entry>
    <!-- 桌面上的显示样式：week（周网格）| today（今日课程）| upcoming（接下来）| next（下一节） -->
    <entry name="viewStyle" type="String">
      <default>week</default>
    </entry>
    <!-- 背景不透明度（0-100）。0 = 完全透明，只看到内容浮在桌面上。 -->
    <entry name="backgroundOpacity" type="Int">
      <default>100</default>
    </entry>
  </group>
</kcfg>
